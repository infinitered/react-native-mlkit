{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint": "^8.0.0",
    "eslint-config-expo-mlkit": "*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "resolutions": {
    "expo-constants": "^14.0.0",
    "@expo/config-plugins": "~6.0.0"
  },
  "name": "expo-mlkit",
  "workspaces": [
    "apps/*",
    "modules/*",
    "packages/*"
  ],
  "packageManager": "yarn@3.6.1"
}
